apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "scavenger-cms.fullname" . }}-test-connection"
  labels:
    {{- include "scavenger-cms.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "scavenger-cms.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
